apiVersion: v1
kind: Service
metadata:
  labels:
    app: blue-app
  name: canary
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: blue-app
  type: NodePort
status:
  loadBalancer: {}
