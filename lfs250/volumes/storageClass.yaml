apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs-gp2 # see PersistentVolumeClaims.yaml (in the same folder) that references this StorageClass
provisioner: ebs.csi.aws.com
parameters:
  type: gp2
  fsType: ext4
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer